import React from "react";
import { Box, Typography } from "@mui/material";

const Saved = () => {
  return (
    <Box sx={{ p: 2, textAlign: "center" }}>
      <Typography variant="h5" color="primary">
        Welcome to Lodgely/Saved 🏠
      </Typography>
    </Box>
  );
};

export default Saved;
